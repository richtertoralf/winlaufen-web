package de.winlaufen.web.bridge.source.winlaufen;

/** Local Bridge Control diagnostic only; never part of canonical data or output contracts. */
public enum ProtocolVariant {
    UNKNOWN,
    CURRENT,
    LEGACY
}
